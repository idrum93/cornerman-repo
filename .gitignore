# Scraped corpora and third-party dumps. These are someone else's data,
# they are large, and they are regenerable. Keep them local.
data/*.csv
data/*.zip
# but keep the two files you maintain by hand
!data/upcoming.txt
!data/aliases.txt
# site/predictions.json IS committed - it is the refresh job's output
*.parquet

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.DS_Store
